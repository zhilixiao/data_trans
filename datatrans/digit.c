#include "digit.h"

extern "C"{
    void digit_kernel(int* reference, 
                         int* 		 data,
                         int* 		 input_v,// vertical input (first column)
                         int         dim,
                         int         penalty,
                         int         loop_exit,
                         int         block_offset)
    {

    }
}